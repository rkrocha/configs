(if
    (contains (application_name) "Spotify")
    (begin
        (set_workspace 5)
	(maximize)
    )
)

(if
    (is (application_name) "Untitled window")
    (begin
        (set_workspace 5)
	(maximize)
    )
)
